# NATS JetStream configuration — durability parity with Kafka acks=all
listen: 0.0.0.0:4222
http: 0.0.0.0:8222

jetstream {
  store_dir: /data
  max_mem: 256MB
  max_file: 10GB
  sync_interval: always
}
